# 단순한 프로덕션 이미지 (빌드 스킵)
FROM node:22-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json 복사
COPY package*.json ./

# 프로덕션 의존성만 설치
RUN npm ci --only=production --silent && \
    npm cache clean --force

# 미리 빌드된 애플리케이션과 소스 복사
COPY .next ./.next
COPY public ./public

# 포트 노출
EXPOSE 3000

# 환경 변수 설정
ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1

# 애플리케이션 시작
CMD ["npm", "start"]